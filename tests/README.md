# Tests live here. See .github/skills/testing/gdscript-testing/SKILL.md.
#
# Layout:
#   tests/unit/                — isolated unit tests (no scene tree)
#   tests/integration/mock/    — scene-tree integration tests, mock mode
#   tests/integration/real/    — require godotty-node (CI nightly only)
#
# Naming: <thing>_<scenario>_test.gd
